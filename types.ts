import { IconType } from "react-icons";


export interface Skill {
    Icon: IconType;
    name: string;
    level: string;
  }

export interface SkillIcon {
    Icon : IconType;
    name : string;
    color : string;
}

